import { View } from "react-native"

export const ChatBar = () => {
  return (
    <View
      style={{
        position: "absolute",
        bottom: 0,
        left: 0,
        right: 0,
        height: 200,
        backgroundColor: "#111",
      }}
    ></View>
  )
}
